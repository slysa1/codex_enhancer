Use this pack when the repo exposes Node.js or TypeScript API or service entrypoints.

- Treat route, request, response, and schema changes as contract-sensitive work that usually needs paired updates.
- Check auth, validation, and error behavior in addition to the normal happy path.
- Keep shared types, API docs, and fixtures aligned when the repo already has them.
- Call out compatibility impact explicitly when route shapes, payloads, or error responses change.
