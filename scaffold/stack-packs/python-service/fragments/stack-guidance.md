Use this pack when the repo is driven by Python project files or service-style Python entrypoints.

- Discover commands from `pyproject.toml`, `requirements.txt`, or existing repo docs before editing workflow guidance.
- Keep validation aligned with the repo's actual test, lint, and typing tools.
- Treat config and entrypoint changes as review-sensitive even when the code diff is small.
- Prefer focused regression coverage around changed behavior over broad speculative tests.
