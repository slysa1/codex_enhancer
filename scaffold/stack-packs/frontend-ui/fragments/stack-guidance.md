Use this pack when the repo includes browser-facing UI surfaces or frontend application structure.

- Review the real user-facing states on the changed surface instead of only the happy path.
- Preserve accessibility basics such as labels, keyboard access, focus handling, and meaningful semantics.
- Keep changes consistent with the repo's existing component patterns, styling system, and responsive behavior.
- Avoid introducing browser automation unless the repo already has it or the task explicitly requires it.
