Use this pack when the repo coordinates multiple packages or apps from one workspace root.

- Start from the workspace map before editing.
- Run the narrowest package or affected check the repo already exposes.
- If shared build config, shared types, or cross-package contracts change, widen validation deliberately.
- Do not rename or replace workspace tooling unless the task explicitly requires it.
